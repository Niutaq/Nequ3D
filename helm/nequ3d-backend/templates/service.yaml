apiVersion: v1
kind: Service
metadata:
  name: {{ include "nequ3d-backend.fullname" . }}
  labels:
    app: {{ include "nequ3d-backend.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "nequ3d-backend.fullname" . }}
